from pathlib import Path

from src.aia_coder.watcher import list_instruction_files


def run(in_dir: Path) -> list[Path]:
    return list_instruction_files(in_dir)


if __name__ == "__main__":
    files = run(Path.home() / "AI" / "AIAgentCoder" / "IN")
    for file in files:
        print(file)
